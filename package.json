{
  "name": "calver-action",
  "version": "0.0.0-private",
  "private": true,
  "description": "Unofficial format-driven CalVer GitHub Action with Rust, Go, and npm release workflows",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/calver.js && node --check src/index.js && node --test"
  }
}
